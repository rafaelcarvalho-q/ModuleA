<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.qranio.sample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:theme">

        <activity
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".ScreeOneDefaultFrameworkActivity"
            android:theme="@style/ModuleATheme"/>

        <activity
            android:name=".ScreeOneCustomFrameworkActivity"
            android:theme="@style/ModuleAThemeApp"/>

        <activity
            android:name=".ListDefaultFrameworkActivity"
            android:theme="@style/ModuleATheme"/>

        <activity
            android:name=".ListCustomFrameworkActivity"
            android:theme="@style/ModuleAThemeApp"/>

    </application>

</manifest>