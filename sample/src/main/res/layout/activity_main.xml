<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <Button
            android:id="@+id/bt_default_framework"
            style="@style/CustomButton.Primary.Block"
            android:text="Tela 01 Framework"/>

        <Button
            android:id="@+id/bt_screem_custom"
            style="@style/CustomButton.Primary.Block"
            android:text="Tela 01 Framework Customizada"/>

        <Button
            android:id="@+id/bt_default_2_framework"
            style="@style/CustomButton.Primary.Block"
            android:text="Tela 02 Framework"/>

        <Button
            android:id="@+id/bt_screem_2_custom"
            style="@style/CustomButton.Primary.Block"
            android:text="Tela 02 Framework Customizada"/>

    </LinearLayout>

</RelativeLayout>
